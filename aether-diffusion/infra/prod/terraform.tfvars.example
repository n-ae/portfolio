# Prod environment example configuration
# Copy this file to terraform.tfvars and fill in your values

# Basic configuration
project_name = "aether-diffusion"
environment  = "prod"

# Cloud provider selection
cloud_provider = "google"  # or "oracle"

# Container image
container_image = "ghcr.io/your-username/aether-diffusion:v1.0.0"

# Google Cloud configuration (if using Google)
google_project_id = "your-gcp-project-id"
# region = "us-central1"  # Optional, defaults to us-central1

# Oracle Cloud configuration (if using Oracle)  
# oracle_compartment_id = "ocid1.compartment.oc1..your-compartment-id"
# oracle_tenancy_id = "ocid1.tenancy.oc1..your-tenancy-id"
# region = "us-ashburn-1"  # Optional, defaults to us-ashburn-1

# Application configuration
port = 8080
log_level = "info"

# Environment variables (sensitive)
environment_variables = {
  YAHOO_CLIENT_ID = "your-yahoo-client-id"
  YAHOO_CLIENT_SECRET = "your-yahoo-client-secret"
  # Add other sensitive environment variables here
}

# Resource configuration (prod defaults - higher resources)
min_instances = 1
max_instances = 100
memory = "512Mi"
cpu = "2000m"

# Production custom domain
custom_domain = "api.yourdomain.com"

# Monitoring (enabled for prod by default)
enable_monitoring = true

# Additional tags
tags = {
  owner = "platform-team"
  cost-center = "production"
  environment = "prod"
}